import styled from 'styled-components';
import { Textarea as TextareaSource } from '@jetpack/Textarea';

const TextareaExtended = styled(TextareaSource)``;

export default function Textarea() {
  return <TextareaExtended />;
}
